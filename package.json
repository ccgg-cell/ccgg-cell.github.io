{
  "name": "artifact-atlas",
  "version": "1.0.0",
  "description": "NC Civil War Field Guide and Relic Hunter Community",
  "scripts": {
    "build": "echo 'Web app ready'",
    "cap:add:android": "capacitor add android",
    "cap:sync": "capacitor sync",
    "cap:open:android": "capacitor open android",
    "cap:build:android": "capacitor copy android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/core": "^5.5.0",
    "@capacitor/android": "^5.5.0",
    "@capacitor/cli": "^5.5.0"
  },
  "devDependencies": {}
}
